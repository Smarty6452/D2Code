import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
})

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: 'D2Code — Find Your Dev Match',
  description:
    'Connect with nearby developers based on your tech stack, skills, and interests. Schedule code dates and build together.',
  keywords: ['developers', 'networking', 'coding', 'tech stack', 'meetup'],
  openGraph: {
    title: 'D2Code — Find Your Dev Match',
    description: 'Connect with nearby developers based on your tech stack, skills, and interests.',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}
